% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/przygotowanie_danych.R
\name{prepare_mcda_data}
\alias{prepare_mcda_data}
\title{Prepare MCDA Data with Advanced Syntax}
\usage{
prepare_mcda_data(
  data,
  syntax,
  alternative_column = NULL,
  aggregation_function = mean
)
}
\arguments{
\item{data}{Data frame containing raw variables}

\item{syntax}{String defining criteria (e.g., "Cost =~ k1 + k2")}

\item{alternative_column}{Name of column identifying alternatives.
If NULL, each row is treated as separate alternative.}

\item{aggregation_function}{Function used to merge expert opinions (default: mean)}
}
\value{
Matrix of dimensions (m x 3n), where m is number of alternatives
}
\description{
Transforms raw survey data into fuzzy decision matrix.
Calculates composite scores based on syntax, scales to 1-9 interval,
aggregates expert responses (if applicable), and performs fuzzification.
}
\examples{
\dontrun{
# Example with city data
syntax <- "Quality =~ Safety + Healthcare; Cost =~ Cost_Living + Housing_Price"
result <- prepare_mcda_data(city_data, syntax, alternative_column = "City")
}
}
